import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class infomenu here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class infomenu extends World
{

    /**
     * Constructor for objects of class infomenu.
     * 
     */
    public infomenu()
    {    
        // Create a new world with 600x400 cells with a cell size of 1x1 pixels.
        super(1200, 710, 1);  
        prepare();
    }
    /**
     * Prepare the world for the start of the program.
     * That is: create the initial objects and add them to the world.
     */
    private void prepare()
    {
        /*equis equis = new equis();
        addObject(equis,1048,136);
        equis.setLocation(1060,140);
        removeObject(equis);
        addObject(equis,1052,137);*/
    }
}
